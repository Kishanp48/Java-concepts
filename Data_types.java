public class Data_types {
    public static void main(String args[])
    {
        // Integer
            // byte (1-byte(8-bits))
            // short (2-byte)
            // int (3-byte)
            // long (4-byte)    -- use l after assigning value (ex: l=23434343434l) to spacify the long value
        
        // Decimal
            // float (4-byte)   -- use f after assigning value (ex: f=2.25f)
            // double (8-byte)  -- by default java consider decimal values as double (use for scientific calculations)

        // Characters
            // char (2-byte)    -- java follows UNICODE system that allows to contains all the characters of all the languages in the world
            //                  -- use single quote('') to use char
            //                  -- use double quote("") to use String

        // Boolean
            // boolean          -- java doesn't support 1,0 as true,false
            //                  -- instead we only use true,false

        /*
        int i = 5;
        byte by = 125;
        short sh = 558;
        long l = 25555111l;

        float f = 5.25f;
        double d = 5.25252525;

        char c = 'k';

        boolean b = true;
         */

    }
}
// 5