public class AdvCalc extends Calc   // All the features of the Calc comes in AdvCalc
{
    // If you don't have the Java file you can still use the Class file of that program.

    public int multi(int n1, int n2)
    {
        return n1*n2;
    }

    public int div(int n1, int n2)
    {
        return n1/n2;
    }

    // Repeat the same code is called Redundancy. and there is not need of it.(It is a crime in coding).
}
